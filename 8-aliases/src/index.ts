import showContactInfo from './utils'

showContactInfo({name: 'Joe', email: 'water'})

let userWithMore = {
  name: 'sally',
  email: 'melon',
  age: 23,
  job: 'worker'
}

showContactInfo(userWithMore)